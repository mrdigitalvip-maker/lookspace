# Conventions

- Nomes: `camelCase` para variáveis, `PascalCase` para componentes.
- Imports: usar aliases definidos em `tsconfig.json`.
- Componentes: um componente por arquivo, tipos em `.types.ts`.
